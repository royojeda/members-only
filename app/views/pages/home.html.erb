<h1>Home</h1>

<% if user_signed_in? %>
  <%= button_to(
  "Log Out",
  destroy_user_session_path,
  method: :delete
) %>
<% else %>
  <%= button_to(
    "Sign In",
    new_user_session_path,
    method: :get
  ) %>
<% end %>
